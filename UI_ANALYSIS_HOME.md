# 首页（Home）设计图分析

## 设计图：home_main.png

### 整体布局

首页采用**社交媒体信息流**设计，类似 Instagram/小红书风格，展示用户创作的照片和视频作品。

### 关键元素

#### 1. 顶部搜索栏
- **毛玻璃面板**（半透明白色）
- **左侧**：搜索图标（🔍）
- **中间**：占位文字 `Search users, tags, or photos...`
- **右侧**：语音搜索图标（🎤）
- **圆角**：16dp
- **背景色**：白色 10% 透明度

#### 2. 作品卡片（Feed Cards）
每个卡片包含：
- **用户头像**（圆形，左上角）
- **用户名**（@Anya_Photography，带认证标记 ✓）
- **作品图片**（16:9 比例，圆角 12dp）
- **右下角水印**：`🦋 Yanbao AI`
- **描述文字**（支持 hashtag）
- **互动数据**：
  - ❤️ 点赞数（1.2k Likes）
  - 💬 评论数（350 Comments）
  - 🔗 分享数（45 Shares）

#### 3. 底部导航栏（Tab Bar）
- **5 个 Tab**：
  1. **Edit**（编辑，滑块图标）
  2. **Album**（相册，图片图标）
  3. **Camera**（相机，中心大圆形按钮，粉色渐变）
  4. **Recommended**（推荐，星星图标）
  5. **My**（我的，个人图标）
- **中心相机按钮**：
  - 大圆形（60dp）
  - 粉色渐变背景
  - 相机图标（白色）
  - 突出显示（高于其他图标）

### 视觉风格

- **背景渐变**：紫色 → 粉色（垂直渐变）
- **卡片背景**：毛玻璃效果（白色 5% 透明度 + 模糊）
- **字体**：
  - 用户名：Medium 粗细
  - 描述文字：Regular 粗细
  - 互动数据：Light 粗细
- **图标颜色**：白色

### 交互逻辑

1. **点击搜索栏**：跳转到搜索页面
2. **点击作品卡片**：查看作品详情
3. **点击用户头像/用户名**：进入用户主页
4. **点击点赞/评论/分享**：执行对应操作
5. **点击底部 Tab**：切换到对应页面
6. **点击中心相机按钮**：打开相机拍照

---

## 代码实现检查

### 现有文件
- `HomeScreen.kt` 存在

### 需要实现的组件

#### 1. TopSearchBar
```kotlin
@Composable
fun TopSearchBar(
    onSearchClick: () -> Unit,
    onVoiceClick: () -> Unit
)
```

#### 2. FeedCard
```kotlin
@Composable
fun FeedCard(
    userAvatar: String,
    userName: String,
    isVerified: Boolean,
    imageUrl: String,
    description: String,
    likesCount: Int,
    commentsCount: Int,
    sharesCount: Int,
    onUserClick: () -> Unit,
    onLikeClick: () -> Unit,
    onCommentClick: () -> Unit,
    onShareClick: () -> Unit
)
```

#### 3. BottomTabBar
```kotlin
@Composable
fun BottomTabBar(
    currentTab: HomeTab,
    onTabSelected: (HomeTab) -> Unit
)

enum class HomeTab {
    EDIT, ALBUM, CAMERA, RECOMMENDED, MY
}
```

### ViewModel 需求

```kotlin
class HomeViewModel : ViewModel() {
    val feedList: StateFlow<List<FeedItem>>
    val currentTab: StateFlow<HomeTab>
    
    fun loadFeed()
    fun refreshFeed()
    fun likeFeed(feedId: String)
    fun shareFeed(feedId: String)
}
```

---

## 实现优先级

### 高优先级
1. ✅ BottomTabBar（导航核心）
2. ⚠️ FeedCard（内容展示核心）
3. ⚠️ TopSearchBar

### 中优先级
4. 下拉刷新
5. 上拉加载更多
6. 点赞动画

### 低优先级
7. 搜索功能
8. 语音搜索
9. 分享功能

---

## 对比结果

**实现状态：** ⚠️ 需要查看现有代码

**下一步：** 读取 `HomeScreen.kt` 并对比
